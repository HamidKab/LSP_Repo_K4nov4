
Brainstorm ways to make the ETLpipeline more object oriented. after redesign the solution so that it has multiple classes (one public class per file). Include a javadoc for each class. the output should be the same as the original output. Once you're done put all these new classes within the ...\assignment3 folder

There seems to be duplicates within the folder remove them

Breakdown the object oriented nature of the files within ..\assignment3